# =============================================================================
# Bobot Backend Environment Variables
# =============================================================================

# Environment mode: "development" or "production"
# Production mode enables strict security checks
ENVIRONMENT=development

# =============================================================================
# Security Configuration (REQUIRED in production)
# =============================================================================

# JWT Secret Key - REQUIRED in production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=your-secret-key-min-32-chars-here

# Super Admin password - REQUIRED in production
# If not set in production, a random password will be generated and printed
ADMIN_PASSWORD=your-secure-admin-password

# =============================================================================
# CORS Configuration
# =============================================================================

# Comma-separated list of allowed origins
# In production, set this to your actual domains
# Example: CORS_ORIGINS=https://app.bobot.se,https://widget.bobot.se
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# =============================================================================
# Demo Data (Optional)
# =============================================================================

# Enable demo company and data (disabled in production by default)
# Set to "true" to create demo company
ENABLE_DEMO_DATA=true

# Demo company password (only used if ENABLE_DEMO_DATA=true)
DEMO_PASSWORD=demo123

# =============================================================================
# Ollama AI Configuration
# =============================================================================

# Ollama API URL (use http://ollama:11434 in Docker)
OLLAMA_BASE_URL=http://localhost:11434

# Ollama model to use
OLLAMA_MODEL=qwen2.5:14b

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite (development)
DATABASE_URL=sqlite:///./bobot.db

# PostgreSQL (production) - uncomment and configure:
# DATABASE_URL=postgresql://user:password@localhost:5432/bobot

# PostgreSQL connection pool settings (only used with PostgreSQL)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# =============================================================================
# Email Configuration
# =============================================================================

# Enable email sending (set to "true" to enable)
EMAIL_ENABLED=false

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_TLS=true

# Sender information
EMAIL_FROM=noreply@bobot.se
EMAIL_FROM_NAME=Bobot

# For Gmail: Use an App Password, not your regular password
# https://support.google.com/accounts/answer/185833

# For SendGrid: Use api.sendgrid.com and your API key
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# =============================================================================
# Error Tracking (Sentry)
# =============================================================================

# Sentry DSN for error tracking (optional)
# Get your DSN from: https://sentry.io
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# =============================================================================
# Production Checklist
# =============================================================================
# Before deploying to production, ensure:
#
# [ ] 1. ENVIRONMENT=production
# [ ] 2. SECRET_KEY is set to a unique, random 64+ character string
# [ ] 3. ADMIN_PASSWORD is set to a strong password
# [ ] 4. CORS_ORIGINS lists only your actual domains
# [ ] 5. ENABLE_DEMO_DATA is not set (defaults to disabled in production)
# [ ] 6. DATABASE_URL points to PostgreSQL (not SQLite)
# [ ] 7. Database is backed up before first startup
# [ ] 8. EMAIL_ENABLED=true with proper SMTP settings
# [ ] 9. SENTRY_DSN is configured for error tracking
# [ ] 10. 2FA is enabled for all admin accounts after first login
# [ ] 11. HTTPS is enforced (via reverse proxy)
# [ ] 12. Regular database backups are configured
